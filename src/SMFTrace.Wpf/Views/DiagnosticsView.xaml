<UserControl x:Class="SMFTrace.Wpf.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SMFTrace.Wpf.Views"
             xmlns:vm="clr-namespace:SMFTrace.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:DiagnosticsViewModel}"
             Background="#252526">

    <UserControl.Resources>
        <Style TargetType="CheckBox" x:Key="FilterCheckBoxStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter Bar -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="8">
            <WrapPanel>
                <TextBlock Text="Show:" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <CheckBox Content="Notes" IsChecked="{Binding ShowNotes}" Style="{StaticResource FilterCheckBoxStyle}"/>
                <CheckBox Content="CC" IsChecked="{Binding ShowControlChanges}" Style="{StaticResource FilterCheckBoxStyle}"/>
                <CheckBox Content="PC" IsChecked="{Binding ShowProgramChanges}" Style="{StaticResource FilterCheckBoxStyle}"/>
                <CheckBox Content="Meta" IsChecked="{Binding ShowMeta}" Style="{StaticResource FilterCheckBoxStyle}"/>
                <CheckBox Content="SysEx" IsChecked="{Binding ShowSysEx}" Style="{StaticResource FilterCheckBoxStyle}"/>
                <CheckBox Content="Other" IsChecked="{Binding ShowOther}" Style="{StaticResource FilterCheckBoxStyle}"/>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>

                <CheckBox Content="Meta Only" IsChecked="{Binding MetaOnlyMode}" Style="{StaticResource FilterCheckBoxStyle}"/>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>

                <TextBlock Text="Search:" Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="150"
                         VerticalAlignment="Center"/>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>

                <CheckBox Content="Auto-scroll" IsChecked="{Binding AutoScrollEnabled}" Style="{StaticResource FilterCheckBoxStyle}"/>
            </WrapPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Event List (Virtualized) -->
            <DataGrid Grid.Column="0"
                      x:Name="EventList"
                      ItemsSource="{Binding FilteredEvents}"
                      SelectedItem="{Binding SelectedEvent}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      EnableRowVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="#1E1E23"
                      Foreground="White"
                      RowBackground="#1E1E23"
                      AlternatingRowBackground="#252526"
                      BorderThickness="0"
                      FontFamily="Consolas"
                      FontSize="12"
                      PreviewMouseDown="EventList_PreviewMouseDown"
                      ScrollViewer.ScrollChanged="EventList_ScrollChanged">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#3C3C3C"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#555"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="50"/>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeDisplay}" Width="80"/>
                    <DataGridTextColumn Header="Tick" Binding="{Binding Tick}" Width="70"/>
                    <DataGridTextColumn Header="Trk" Binding="{Binding Track}" Width="40"/>
                    <DataGridTextColumn Header="Ch" Binding="{Binding Channel}" Width="35"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding EventType}" Width="120"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding Summary}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          Background="#3C3C3C"/>

            <!-- Details Pane -->
            <Border Grid.Column="2" Background="#1E1E23" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Event Details"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               Margin="0,0,0,8"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding DetailText, Mode=OneWay}"
                                 IsReadOnly="True"
                                 Background="Transparent"
                                 Foreground="LightGray"
                                 BorderThickness="0"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
